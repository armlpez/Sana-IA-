{
    "commands":  [
                     "echo === PROCESSES ===",
                     "ps aux --sort=-%mem | head -n 15",
                     "echo === DOCKER === ",
                     "docker ps"
                 ]
}
